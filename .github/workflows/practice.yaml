name: workflow
on:
  push:
    branches:
      - main
      - "!feature/*"

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        images: [nginx]
    runs-on: ${{ matrix.os }}
    steps:
          - name: running on ${{matrix.os}}
            run: echo "this image ${{matrix.images}}"

            
  test2:
    runs-on: ubuntu-latest
    steps: 
        - name: 


